package name.kingdoms;

/**
 * Marker interface for blocks that "own" spawned kingdom entities.
 * Entities bound to a spawner block can despawn when the spawner is destroyed.
 */
public interface IKingdomSpawnerBlock {
    // marker only
}